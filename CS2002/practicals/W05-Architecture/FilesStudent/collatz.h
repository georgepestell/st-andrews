#include <stdbool.h>
#include <limits.h>

#define MAXARRAYSIZE 200
/* Return whether elem occurs in array, between left boundary (inclusive) 
 * and right boundary (exclusive).
 */
bool collatz(int, int, int, int, int*, int*);
